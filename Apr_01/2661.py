import sys
# sys.stdin = open('.txt', 'r')
input = sys.stdin.readline


def is_good(s):



def perm(i, p):
    if i == N:
        for length in range((N//2)+1):
            for idx in range(0, N, length):
                if p[idx+length] == p[idx]
            p = int(''.join(map(str, p)))
            result.append(p)
        return

    for j in range(1, 4):
        perm(i+1, p+[j])


N = int(input())

result = []

perm(0, [])

print(result)